/** 实现文件在 `KeyBoard/keyboard.tsx`（大写 B），改 `keyboard/keyboard` 小写路径在部分环境下不会生效。 */
export { default } from '@/components/KeyBoard/keyboard';
export type { TravelVirtualKeyboardProps, PatternKey, CompositeLayoutKey } from '@/components/KeyBoard/types';


